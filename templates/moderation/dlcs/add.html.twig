{% extends 'moderation/base.html.twig' %}

{% set sectionTitle = 'Add a new DLC' %}

{% block moderationContent %}
    <section class="section-moderator-add">
        <div class="box">
            <div class="title">Add new DLC</div>
            {{ form_start(addForm) }}
            {{ form_widget(addForm) }}
            {{ form_end(addForm) }}
        </div>
    </section>
{% endblock %}